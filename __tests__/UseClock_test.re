// open Jest;
// open Expect;
// open ReasonHooksTestingLibrary.Testing;
// open UseClock;
// describe("useClock", () => {
//   // open State;
//   open Result;
//   let container = renderHook(() => UseClock.useClock(), ());
//   test("clock works", () =>
//     expect(container->result->current.date) |> toEqual(Js.Date.now())
//   );
  // test("date is 1", () => {
  //   act(() => container->result->current.set(prev => prev + 1));
  //   expect(container->result->current.date) |> toEqual(1);
  // });
  // test("date is 2", () => {
  //   act(() => container->result->current.set(prev => prev + 1));
  //   expect(container->result->current.date) |> toEqual(2);
  // });
});